# Core Service (Spring Boot)
This directory will contain the Spring Boot application for managing Users and Jobs.
Run `docker-compose up` to start the PostgreSQL database before running the Spring Boot app.