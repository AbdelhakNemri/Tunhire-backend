# AI Service (FastAPI)
This directory will contain the FastAPI application for matching candidates to jobs and parsing CVs.
Run `docker-compose up` to start the PostgreSQL database before running the FastAPI app.